

spring:
  application:
    name: a-maker

  datasource:
    username: sa
    password:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test;MODE=MySQL

  jpa:
    database: mysql
    hibernate:
      ddl-auto: create